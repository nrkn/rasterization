import { PointAction } from './types';
export declare const circle: (cx: number, cy: number, radius: number, action: PointAction) => void;
export declare const circleQuadrant: (radius: number, action: PointAction) => void;
